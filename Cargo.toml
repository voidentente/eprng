[package]
name = "eprng"
version = "0.1.0"
edition = "2021"
authors = ["voidentente <voidentente@paranoici.org>"]
repository = "https://github.com/voidentente/eprng"
description = "Extremely Pseudo RNG"
license = "MIT"

[features]
default = ["distribution"]
distribution = []